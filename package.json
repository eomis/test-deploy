{
  "name": "test-deploy",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "aladin dev repo",
    "build": "aladin build repo",
    "deploy": "aladin deploy",
    "deploy:release": "aladin deploy release"
  },
  "dependencies": {
    "@tencent/aladin-repo-proxy": "^0.0.8",
    "vue": "^3.2.47"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.1.0",
    "typescript": "^4.9.3",
    "vite": "^4.2.0"
  }
}